<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nieelitarni.besthackathon2019">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NewTaskActivity"
            android:label="@string/title_activity_new_task"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".InitUserActivity"
            android:label="@string/title_activity_init_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MessagesActivity"
            android:label="@string/title_activity_messages"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Debug_RemoveMe"
            android:label="@string/title_activity_debug__remove_me"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TaskInfoActivity" />
        <activity android:name=".TaskCommitsActivity" />
        <activity android:name=".TaskMessagesActivity" />
        <activity android:name=".TaskUsersActivity" />
        <activity
            android:name=".NavigationActivity"
            android:label="@string/title_activity_navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>